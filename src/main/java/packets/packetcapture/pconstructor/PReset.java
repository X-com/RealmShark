package packets.packetcapture.pconstructor;

/**
 * Reset interface used in packet constructor for sending reset updates when
 * receiving reset packets.
 */
public interface PReset {
    void reset();
}
